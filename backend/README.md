<!--
    *DevCollab, a platform where developers:

    - Create projects
    - Join collaborations
    - Track tasks

    Flow of the application:

    Login => Projects => Tasks

    *For the backend, we will be using:

    Node.js + Express
    MongoDB
    JWT Auth
    Bcrypt
    Middleware
    REST API

    Models:

    - User
    - Project
    - Task
    - Full CRUD (Project + Task)

    *For the frontend, we will be using:

    Angular

    - Auth UI
      Login
      Register

    - Project UI
      project-list
      project-detail

    - Task UI
      task-list

    For the backend: Logic + Data
    For the frontend: UI + API calls

    *WebSockets:

    I need at least 4 events, examples:

    task:created
    task:updated
    task:deleted
    project:created

    *For the deployment using Render:

    Backend => Web Service
    Frontend => Static Site

    *Video demo for the project:

    I must show:

    Auth flow
    CRUD
    Real-time updates
-->
